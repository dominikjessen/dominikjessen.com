---
import '@fontsource-variable/signika';
// import '@fontsource-variable/josefin-sans';
// import '@fontsource/ubuntu';
// import '@fontsource-variable/inter';

import Nav from '../components/structure/Nav.astro';
import Footer from '../components/structure/Footer.astro';
import ThemeToggle from '../components/ui/ThemeToggle.astro';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="bg-background text-foreground overflow-x-hidden">
    <Nav />
    <ThemeToggle className="absolute right-[4rem] top-[1.25rem]" />
    <main class="w-5/6 mx-auto pt-8 pb-24">
      <slot />
    </main>
    <Footer />
  </body>
</html>

<style is:global>
  html {
    --background: 72 99% 98%;
    --foreground: 70 5% 7%;
    --foreground_lighter: 70 11% 11%;
    --primary: 119 19% 42%;
    --accent_seagreen: 150 66% 75%;
    --brass: 43 66% 66%;
    --rose: 0 87% 91%;
    --mint: 150 66% 97%;
    --blush: 0 87% 97%;
    --aliceblue: 208 100% 97%;
  }

  html.dark {
    --background: 70 5% 7%;
    --foreground: 72 99% 98%;
    --foreground_lighter: 72 99% 100%;
    --primary: 119 19% 42%; /* Might have to be lighter */
    --accent_seagreen: 150 66% 75%; /* TBD */
    --brass: 43 66% 66%; /* TBD */
    --rose: 0 87% 91%; /* TBD */
    --mint: 150 66% 97%; /* TBD */
    --blush: 0 87% 97%; /* TBD */
    --aliceblue: 208 100% 97%; /* TBD */
  }

  ::selection {
    background: hsl(var(--primary));
    color: hsl(var(--background));
  }

  h1,
  h2 {
    font-family: 'Signika Variable', sans-serif;
    font-weight: 700;
  }

  a:hover {
    opacity: 80%;
  }

  .scroll-fade-in {
    animation: fade-in 2s linear both;
    animation-timeline: view();
    animation-range: entry 20% cover 50%;
  }

  @keyframes fade-in {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
</style>
